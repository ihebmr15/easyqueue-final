{% extends 'base.html.twig' %}

{% block title %}Home - EasyQueue{% endblock %}

{% block body %}
<h1>Welcome to EasyQueue!</h1>
<p style="font-size: 18px; margin-bottom: 30px;">Your efficient queue management system</p>

<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin-top: 30px;">
    <div class="service-card">
        <h2>📋 View Services</h2>
        <p>Browse all available services and book your ticket</p>
        <a href="{{ path('services') }}" class="btn">View Services</a>
    </div>
    
    <div class="service-card">
        <h2>🎫 My Tickets</h2>
        <p>Check the status of your tickets and estimated wait times</p>
        <a href="{{ path('user_dashboard') }}" class="btn">My Dashboard</a>
    </div>
    
    {% if 'ROLE_ADMIN' in app.user.roles %}
    <div class="service-card">
        <h2>⚙️ Admin Panel</h2>
        <p>Manage users, services, and monitor the queue system</p>
        <a href="{{ path('admin_dashboard') }}" class="btn">Admin Dashboard</a>
    </div>
    {% endif %}
</div>
{% endblock %}

